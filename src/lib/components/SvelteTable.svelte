<script>
	import SvelteTable from 'svelte-table';
	const rows = [
		/** data (example below) */
	];
	const columns = [
		/** columns config (example below) */
	];
</script>

<SvelteTable {columns} {rows} />
